{
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.1.0"
  },
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}